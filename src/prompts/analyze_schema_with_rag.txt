Phân tích câu hỏi và xác định bảng cần query trong database.

Câu hỏi: {question}

Các bảng liên quan (từ RAG):
{retrieval_info}

Chi tiết Schema của các bảng:
{schema_context}

Nhiệm vụ QUAN TRỌNG:
1. **Xác nhận bảng chính xác** - Review RAG suggestions nhưng tự verify:
   - Bảng có chứa data cần thiết không?
   - VD: consumption data → yearmonth table, không phải customers table
   - Có cần JOIN không? Tránh JOIN thừa
   
2. **Phân tích data types và formats**:
   - Date columns: YYYYMM string vs YYYY-MM-DD date vs timestamp
   - Numeric vs String columns
   - Check column names chính xác (case-sensitive)
   
3. **Xác định logic aggregation**:
   - "Highest/lowest trong period" → SUM/AVG + GROUP BY + ORDER BY + LIMIT
   - "Max/min value" → MAX/MIN function
   - "Average per time period" → tính toán cẩn thận (AVG vs SUM/count)
   
4. **Date handling strategies**:
   - YYYYMM format: SUBSTR(date, 1, 4) = 'YYYY' và SUBSTR(date, 5, 2) = 'MM'
   - Date type: date >= '...' AND date <= '...'
   - BETWEEN chỉ dùng cho numeric/date types

Format trả về (JSON format):
```json
{{
  "tables": ["table1", "table2"],
  "primary_columns": {{"table1": ["id", "name"], "table2": ["id"]}},
  "foreign_keys": [{{"from": "table1.fk_id", "to": "table2.id", "relationship": "many-to-one"}}],
  "join_paths": ["table1 JOIN table2 ON table1.fk_id = table2.id"],
  "where_conditions": ["column1 IS NOT NULL", "column2 = value"],
  "order_by": ["created_at DESC"],
  "date_format": "YYYYMM string hoặc YYYY-MM-DD date hoặc timestamp",
  "aggregation_logic": "MAX/MIN/SUM+GROUP BY/AVG - giải thích logic",
  "summary": "các bảng và cột cần dùng",
  "notes": "LƯU Ý về date format, aggregation logic, và edge cases"
}}
```

